/**
 * Intentionally consume the value. This is a function to be explicit
 * that we're using the value, so that eslint doesn't complain about
 * an unused expression.
 */
export function consume(_value: unknown) {}
