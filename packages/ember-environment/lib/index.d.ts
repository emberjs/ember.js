export const environment: {
  hasDOM: boolean;
  isChrome: boolean;
  isFirefox: boolean;
  location: Location | null;
  history: History | null;
  userAgent: string;
  window: Window | null;
}

export const ENV: {
  _ENABLE_RENDER_SUPPORT: boolean;
};
